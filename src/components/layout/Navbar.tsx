
import logo from '../../assets/logo.svg';

export const Navbar = () => {
    return (
        <nav>
            <img src={logo} alt="Logo" />
            <div>
                <h3>nav</h3>
            </div>
        </nav>
    )
}

export default Navbar;